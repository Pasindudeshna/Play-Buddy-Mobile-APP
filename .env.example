# Copy this file to .env and fill in real values. .env is gitignored.

# Base URL of the deployed matchmaking/venue API (see netlify/functions).
# Optional — defaults to the production Netlify site in lib/matchApi.ts.
# Only set this to point at a local `netlify dev` server or a different deploy.
EXPO_PUBLIC_MATCH_API_URL=
